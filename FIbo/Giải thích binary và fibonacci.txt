3. Tại sao findBound lại để private?

Ý nghĩa "private": chỉ được gọi bên trong class chứa nó, không cho các class khác gọi trực tiếp.

findBound chỉ là hàm phụ trợ (helper method), không cần thiết phải public vì:

Người dùng bên ngoài chỉ cần dùng binarySearch là đủ.

Nếu để public, sẽ làm lộ logic nội bộ, dễ gây lạm dụng sai mục đích.

Giữ nguyên tắc Encapsulation (đóng gói) trong OOP: chỉ public những gì cần cho "người dùng" của class, còn helper thì private để code rõ ràng và an toàn hơn.

Ví dụ thực tế:

Bạn đi siêu thị chỉ cần đưa tờ tiền → nhận hóa đơn.

Bạn không cần (và không nên) biết nhân viên nội bộ đi kiểm kho như thế nào.
→ binarySearch là dịch vụ công khai, findBound chỉ là bước xử lý nội bộ.

------------------------------------------------------------------------------------------------
🔹 Trường hợp findBound(..., true) → tìm first bound

Lúc đầu: left = 0, right = 4

B1: mid = (0+4)/2 = 2 → a[2] = 2 ✅

result = 2

Vì findFirst = true → ép sang bên trái: right = mid - 1 = 1.

B2: mid = (0+1)/2 = 0 → a[0] = 1 < target

left = mid+1 = 1.

B3: mid = (1+1)/2 = 1 → a[1] = 2 ✅

result = 1

ép tiếp sang trái: right = 0.

Kết thúc (left > right) → return result = 1.
👉 Tìm được chỉ số đầu tiên.

🔹 Trường hợp findBound(..., false) → tìm last bound

Lúc đầu: left = 0, right = 4

B1: mid = 2 → a[2] = 2 ✅

result = 2

Vì findFirst = false → ép sang bên phải: left = mid+1 = 3.

B2: mid = (3+4)/2 = 3 → a[3] = 2 ✅

result = 3

ép tiếp sang phải: left = 4.

B3: mid = 4 → a[4] = 3 > target

right = mid-1 = 3.

Kết thúc (left > right) → return result = 3.
👉 Tìm được chỉ số cuối cùng.

-----------------------------
Hai trường hợp không chạy song song mà chạy tuần tự:

Đầu tiên gọi findBound(..., true) để tìm biên trái.

Nếu không tìm thấy (= -1) → trả về list rỗng luôn, không cần tìm tiếp.

Nếu tìm thấy → lưu vào findFirst.

Sau đó mới gọi findBound(..., false) để tìm biên phải.

Cuối cùng, từ findFirst đến findLast, gom các chỉ số lại.


int findFirst = findBound(a, target, left, right,  true);
if (findFirst == -1) return new ArrayList<>();
int findLast = findBound(a, target, left, right, false);

----------------------------------------------------------------------------------------------
Regex là gì?

Regex (Regular Expression – Biểu thức chính quy) là một ngôn ngữ mô tả mẫu chuỗi (string pattern).

Nó không phải "tự có trong Java", mà là một chuẩn chung (PCRE – Perl Compatible Regular Expressions) được rất nhiều ngôn ngữ hỗ trợ (Java, Python, JavaScript, C#, PHP…).

Regex được dùng để:

Kiểm tra chuỗi có hợp lệ không (validation)

Tìm kiếm (search)

Thay thế (replace)

Tách chuỗi (split)

👉 Nói đơn giản: regex là một chuỗi ký tự đặc biệt để định nghĩa mẫu (pattern).