
LAB211 Assignment
Type:
Long Assignment

Code:
J1.S.P0025

LOC:
175

Slot(s):
5
Title 
Create a Java console program to normalize text.
Background Context
N/A
       
Program Specifications
The program provides shop owners tools to run their business effectively. The program s functions as below:

Function details:
The program read text file (input.txt) and normalize text and sentences. After that write normalized document to the output file (output.txt).
- Only one space between words.
- Only one space after comma (,), dot (.) and colon (:).
- There are no spaces before and after sentence or word phrases in quotes (" ").

- First character of word after dot is in Uppercase and other words are in lower case.
- First character of word in first line is in Uppercase.

- There are no blank line between lines
- There are no space between comma or dot and word in front of it. 
- Must have dot at the end of text.
The program have to use Exception to handle exception when read or write (file not found, cannot read or write exception, etc.

Example as below:
Document before normalizing:
as you can see,Detecting whether a string is normalized can be quite efficient. A lot of the cost of normalizing in the"    second row    "is for the initialization of buffers.The cost of which is amortized when one is processing larger strings.

 As it turns out, These buffers are rarely needed, so we may change the implementation at some point to speed up the common case for small strings even further

Document after normalizing:
As you can see, detecting whether a string is normalized can be quite efficient. A lot of the cost of normalizing in the "second row" is for the initialization of buffers, the cost of which is amortized when one is processing larger strings. 
As it turns out, these buffers are rarely needed, so we may change the implementation at some point to speed up the common case for small strings even further.

Technical Requirements
1. Using Object-Oriented programming style.
2. Use only core Java functions and classes.

Guidelines

Slot
Task
Description
1
- Design Code
- Read text file
- Should use BufferReader, StringBuffer 
2
- Normalize content file
- Only one space between words.
- Only one space after comma (,), dot (.) and colon (:). First character of word after dot is in Uppercase and other words are in lower case
3
- Normalize content file
- There are no spaces before and after sentence or word phrases in quotes (  ).
- First character of word in first line is in Uppercase.
4
- Normalize content file
- There are no space between comma or dot and word in front of it. 
- Must have dot at the end of text.
- Write result to output file
5
- Review program



### Danh sách test case đề xuất (ngắn gọn, đủ bao phủ)

- **Khoảng trắng giữa từ**
  - Input: `a   b\t\tc` → Output: `A b c.`
  - Input: `  nhiều   space   ` → Output: `Nhiều space.`

- **Khoảng trắng trước/sau dấu câu [, . : ; ! ?]**
  - Input: `a ,  b` → `A, b.`
  - Input: `a.   b` → `A. B.`
  - Input: `a:b` → `A: b.`

- **Không có khoảng trắng trước dấu câu**
  - Input: `word ,` → `Word,`
  - Input: `word .` → `Word.`

- **Trích dẫn (quotes)**
  - Input: `"   inside   "` → `"Inside".`
  - Input: `word"   in   "` → `Word "in".`
  - Input: `"a" , "b"` → `"A", "b".`
  - Input: `("  text  ")` → `("text").`

- **Viết hoa sau dấu chấm, và toàn câu thường trừ chữ đầu**
  - Input: `as you CAN SEE. detecting IS HARD.` → `As you can see. Detecting is hard.`
  - Input: `a! b? c.` → `A. B. C.` (đã chuẩn hóa !/? → . ở cuối câu)

- **Chữ đầu văn bản viết hoa**
  - Input: `   hello world` → `Hello world.`

- **Loại bỏ dòng trống**
  - Input:
    ```
    line1

     

    line2
    ```
    → 
    ```
    Line1.
    Line2.
    ```

- **Giữ đúng 1 newline giữa các dòng, không có dòng trắng xen kẽ**
  - Input có nhiều `\n\n\n` → chỉ còn một `\n` giữa các dòng.

- **Dấu chấm cuối văn bản**
  - Input: `ending` → `Ending.`
  - Input: `ending!` → `Ending.` (thay `!` bằng `.`)
  - Input: `ending.` → giữ nguyên một dấu `.`

- **Dấu câu cạnh dấu ngoặc kép/ngoặc tròn**
  - Input: `word,"text"` → `Word, "text".`
  - Input: `word."text",next` → `Word. "Text", next.`

- **Tab, khoảng trắng đặc biệt**
  - Input với tab `\t` và nhiều space → tất cả thu gọn còn 1 space.

- **Unicode/tiếng Việt có dấu**
  - Input: `đÂy LÀ ví Dụ.` → `Đây là ví dụ.`
  - Input nhiều câu tiếng Việt có dấu để đảm bảo regex không phá ký tự.

- **Trường hợp rỗng/chỉ có whitespace**
  - Input: `""` hoặc `"   \n \t"` → In ra cảnh báo “File is null or don't have anything.” và không ghi file.

- **Nhiều dòng và đoạn văn**
  - Input hai đoạn nhiều dấu câu lẫn nhau; kiểm tra:
    - Mỗi câu cách nhau 1 space sau dấu chấm.
    - Không có space trước dấu câu.
    - Chữ đầu mỗi câu viết hoa.

- **Edge punctuation**
  - Input: `a... b?? c!!` → chuẩn về: `A. B. C.`

- **File/Exception**
  - File không tồn tại: ném và bắt `NoSuchFileException` → in thông báo “No found the file …”.
  - Lỗi quyền ghi (không thể ghi `output.txt`) → in “Error when read or write file: input.txt”.
  - File lớn: xác nhận chương trình vẫn chạy ổn (BufferedReader).

Bạn muốn mình tạo nhanh một bộ input mẫu tương ứng và expected output ngắn trong thư mục `test` để thầy dễ chạy đối chiếu không?
